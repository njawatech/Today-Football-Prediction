/* jshint esversion: 6 */
( function ( $ ) {
	'use strict';

	$( document ).ready( function () {
		// Future enhancements: pagination, live refresh, filtering.
		console.log( 'Today Football Prediction scripts loaded.' );
	} );
} )( jQuery );
